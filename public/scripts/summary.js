/*import { pipeline } from "@xenova/transformers";

const pipe = await pipeline("summarization");

export async function getAndDisplaySummary(inputText) {
    const result = await pipe(inputText, { max_length: 100, min_length: 50 });
    return result[0].summary_text;
}*/
